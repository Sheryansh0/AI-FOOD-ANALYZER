# Google Gemini API Key (required for food analysis)
GEMINI_API_KEY=your_gemini_api_key_here

# Flask Configuration
FLASK_ENV=production
PORT=8000

# Optional: Set confidence threshold for local models
MODEL_CONFIDENCE_THRESHOLD=0.7
